generator gameClient {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/game-client"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_GAME_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model announcement {
  platform     Int      @id
  announcement String   @db.LongText
  time         DateTime @default(dbgenerated("'2019-01-01 00:00:00'")) @db.DateTime(0)

  @@index([platform], map: "ind_e_platform")
}

